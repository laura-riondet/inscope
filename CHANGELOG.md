# Changelog

## [Unreleased]

### Added
- Google OAuth 2.0 sign-in via Google Identity Services
- Unread email grouping by sender with counts
- Client-side sender filter
- Per-sender detail view with thread list
- Find unsubscribe link (List-Unsubscribe header parsing)
- One-click unsubscribe (mailto: silent send, https: new tab)
- Bulk delete all emails from a sender (moves to Gmail Trash)
- Per-thread expand, single delete, double-click to delete
- Scope-out delete animation with reticle
- Skeleton loading states and empty states
- Privacy-first: no backend, tokens in sessionStorage only
- CSP headers via vercel.json
